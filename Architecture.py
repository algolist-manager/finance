"""
백테스팅
"""
backT(target_dataset, algo)


"""
초기 설정값
"""
raw_data

set_data

init_portfolio = init_set(set_data)

execution_query = get_query(estim_result)

st_execute(execution_query)

dbmanager = Datamanager.

decider = Decider(data, method = 'MA')
decision = decider.run()
QUERY =  decider.to_query(decision)
buyer = Buyer()
buyer.get_query(query)
buyer.request()

"""
일정 주기(장중 30분)마다 실행
"""
day_tmp = buy_check()

execution_query = get_query(estim_result)

st_execute(execution_query)


